# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
build/
dist/
*.egg-info/

# Virtual environments
venv/
ENV/
env/
.venv/

# IDE/editor directories
.vscode/
.idea/

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Unit test / coverage
.pytest_cache/
coverage.xml

# Jupyter Notebook checkpoints
.ipynb_checkpoints

# Python data files
*.npz

# Logs and databases
*.log
*.sqlite3

# macOS
.DS_Store

# Profiler
*.prof

# Acados
acados_example_mhe/
c_generated_code/
acados_mhe.json

# Acados Generation
# Ignores any directory starting with "c_generated_code" (e.g., c_generated_code_ball_mpc)
c_generated_code*/

# Ignores the compiled object files generated during the build
*.o

# Ignores specific Acados JSON descriptors (adjust if you have other JSON config files)
# This matches ball_mpc.json, acados_mhe.json, etc.
*_mpc.json
acados_*.json

# Ignores shared libraries (if not already covered by *.so above)
*.dylib